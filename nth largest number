def find_nth_largest(num_list, n):
    sorted_list = sorted(num_list, reverse=True)
    return sorted_list[n-1]
num_list = [10, 20, 5, 30, 15]
n = 3
nth_largest = find_nth_largest(num_list, n)
print(f"The {n}th largest number in the list is: {nth_largest}")
